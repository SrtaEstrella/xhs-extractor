# 📖 小红书笔记提取工具 - 使用说明

## 🎯 适合人群

本工具专为**不熟悉编程**的用户设计，所有操作都可以通过**双击文件**完成，无需输入任何命令。

---

## 📥 第一步：下载项目

1. 访问项目GitHub页面
2. 点击右上角绿色的 **"Code"** 按钮
3. 选择 **"Download ZIP"** 下载压缩包
4. 解压下载的文件到任意位置（如桌面）

---

## 🚀 第二步：一键启动（最简单）

### macOS 用户

1. 找到解压后的文件夹
2. 找到 `一键启动_mac.sh` 文件
3. **双击运行** `一键启动_mac.sh` 文件
4. 如果提示"无法打开"，请：
   - 右键点击 `一键启动_mac.sh`
   - 选择"打开方式" → "终端"
   - 或在终端中执行：`chmod +x 一键启动_mac.sh && ./一键启动_mac.sh`

### Windows 用户

1. 找到解压后的文件夹
2. 找到 `一键启动_win.bat` 文件
3. **双击运行** `一键启动_win.bat` 文件

### 一键启动会自动完成：

✅ **自动检查并安装依赖**（首次使用时）
- 如果缺少Python包，会自动安装
- 如果缺少浏览器，会自动下载

✅ **自动检查登录状态**
- 如果已登录，直接启动
- 如果未登录，会引导你完成登录（只需一次）

✅ **自动启动Web界面**
- 浏览器会自动打开工具界面
- 在界面中输入小红书链接即可使用

> 💡 **提示**：
> - 首次使用可能需要几分钟安装依赖，请耐心等待
> - 登录只需要做一次，之后会自动保存登录状态
> - 之后使用只需双击 `一键启动_mac.sh`（macOS）或 `一键启动_win.bat`（Windows）即可

### 方式2：命令行（可选）

如果你熟悉命令行，也可以使用命令行工具：

```bash
# macOS/Linux
python3 -m xhs_extractor_module.cli

# Windows
python -m xhs_extractor_module.cli
```

---

## ❓ 常见问题

### Q: 双击文件没有反应？

**macOS:**
- 右键点击文件 → 选择"打开方式" → "终端"
- 或在终端中执行：`chmod +x 文件名.sh && ./文件名.sh`

**Windows:**
- 确保文件扩展名是 `.bat` 而不是 `.txt`
- 如果系统提示安全警告，选择"仍要运行"

### Q: 提示"未找到Python"？

**解决方案：**
1. 访问 https://www.python.org/downloads/
2. 下载并安装Python 3.8或更高版本
3. 安装时勾选"Add Python to PATH"（Windows用户）
4. 重新运行安装脚本

### Q: 安装很慢或失败？

**解决方案：**
1. 检查网络连接
2. 如果在中国大陆，可能需要使用代理或等待更长时间
3. 可以手动执行安装命令（见README.md）

### Q: 登录后还是提示未登录？

**解决方案：**
1. 重新运行登录脚本
2. 确保在浏览器中成功登录
3. 登录后回到终端按回车键确认

---

## 📞 需要帮助？

如果遇到问题：
1. 查看项目README.md文件
2. 在GitHub上提交Issue
3. 查看常见问题解答

---

**祝使用愉快！** 🎉

